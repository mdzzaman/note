import { CreateCategoryDialogPage } from './create-category-dialog.po';

describe('note App', () => {
  let page: CreateCategoryDialogPage;

  beforeEach(() => {
    page = new CreateCategoryDialogPage();
  });

  // it('should display welcome message', () => {
  //   page.clickOnAddCategory(3);
  // });
});
